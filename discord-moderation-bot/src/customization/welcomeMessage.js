const welcomeMessage = {
  sendWelcomeMessage: (user) => {
    // Logic to send a welcome message to the user
    console.log(`Welcome to the server, ${user}! We're glad to have you here.`);
  }
};

export default welcomeMessage;